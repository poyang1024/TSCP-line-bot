FROM node:18-alpine

# 安裝 curl 用於健康檢查
RUN apk add --no-cache curl

WORKDIR /app

# 複製 package files
COPY package*.json ./

# 安裝所有依賴（包含 devDependencies 用於開發）
RUN npm ci

# 複製源碼
COPY . .

# 建立上傳目錄
RUN mkdir -p uploads

# 暴露端口
EXPOSE 3000

# 開發模式：使用 nodemon 監控文件變化
CMD ["npm", "run", "dev:watch"]
